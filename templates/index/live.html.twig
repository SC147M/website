{% extends 'base.html.twig' %}

{% block title %}Live{% endblock %}

{% block body %}
    <div class="map">
        <div class="container main-container">
            <div class="row">
                <div class="col-12 mt-4">
                    <h1>Live aus dem Club</h1>
                </div>

                <div class="col-12 mt-4">
                    <style>
                        /* Set the default width to 100% */
                        iframe {
                          width: 100%;
                          height: 800px;
                          border: none; /* Optional: Remove border if not needed */
                        }

                        /* Adjust the width for smaller screens (e.g., mobile) */
                        @media (max-width: 508px) {
                          iframe {
                            width: 100%;
                            height: 800px;
                          }
                        }
                    </style>
                    <iframe src="https://sc147.myscoreboard.org/" frameborder="0" allowfullscreen></iframe>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
