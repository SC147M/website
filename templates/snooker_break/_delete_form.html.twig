<form method="post" action="{{ path('snooker_break_delete', {'id': snooker_break.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ snooker_break.id) }}">
    <button class="btn">Delete</button>
</form>
