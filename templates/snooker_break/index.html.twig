{% extends 'base.html.twig' %}

{% block title %}Breaks{% endblock %}

{% block body %}
    <div class="club">
        <div class="container main-container">
            <div class="row">
                <h2>Breaks</h2>
            </div>

            <div class="row">
                <div class="col-md-6 mt-4">
                    <h3>Aktuelle 25+ Breaks {% if is_granted('ROLE_USER') %}<a class="btn btn-success" href="{{ path('snooker_break_new') }}">Neues Break eintragen</a>{% endif %}</h3>
                    {{ include('snooker_break/_breaktable.html.twig', {'breaks': snooker_breaks}) }}
                </div>

                <div class="col-md-6 mt-4">
                    <h3>Top 20 Breaks im SC147</h3>
                    {{ include('snooker_break/_breaktable.html.twig', {'breaks': highest_breaks}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
